import styled from "styled-components"
import {StyledInputLogin} from "assets/js/login"
import {green} from "../../abstracts/colors"
import {StyledDefaultButton} from "../../components/buttons";

export const StyledInputCaptcha = styled(StyledInputLogin)`
            padding-left:10px!important;
`



export const StyleRereshButton = styled(StyledDefaultButton)`
               background-color:${green[0]};

`
