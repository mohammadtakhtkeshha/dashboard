import styled from "styled-components"

export const StyledFigure = styled.figure`
        & .highcharts-container{
            & svg {
                & .highcharts-credits{
                        display:none;
                }
            }
         }
`
